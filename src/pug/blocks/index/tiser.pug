.tiser
    article#splideTiser.splide
        .splide__track
            ul.splide__list
                each i in Array(4).fill(null).map((_,i)=>i)
                    li.splide__slide 
                        .tiser__wrapper
                            .tiser__block
                                each i in tiser.text
                                    p.tiser__text= i
                        img.tiser__img(src=tiser.img alt="tiser")